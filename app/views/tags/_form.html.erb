<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="dsdserror_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :v_title %><br>
    <%= f.text_field :v_title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
