<%= form_for(@followuptype) do |f| %>
  <% if @followuptype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@followuptype.errors.count, "error") %> prohibited this followuptype from being saved:</h2>

      <ul>
      <% @followuptype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
    <%= f.label :v_title %><br>
    <%= f.text_field :v_title %>
  </div>
   <div class="field">
    <%= f.label :v_color %><br>
    <%= f.text_field :v_color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
